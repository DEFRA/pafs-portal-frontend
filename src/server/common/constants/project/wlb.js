/**
 * Project types for which WLB is mandatory.
 */
export const WLB_MANDATORY_PROJECT_TYPES = ['DEF', 'REF', 'REP']

/**
 * Project types for which WLB is optional.
 */
export const WLB_OPTIONAL_PROJECT_TYPES = ['ELO', 'HCR']

/**
 * Project types for which WLB is hidden (skip to overview).
 */
export const WLB_HIDDEN_PROJECT_TYPES = ['STR', 'STU']
