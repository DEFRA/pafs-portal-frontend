import {
  createAll,
  Button,
  Checkboxes,
  ErrorSummary,
  Header,
  PasswordInput,
  Radios,
  SkipLink
} from 'govuk-frontend'

createAll(Button)
createAll(Checkboxes)
createAll(ErrorSummary)
createAll(Header)
createAll(PasswordInput)
createAll(Radios)
createAll(SkipLink)
